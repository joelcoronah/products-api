export type MetaPaginate = {
  itemCount: number;
  totalItems: number;
  itemsPerPage: number;
  totalPages: number;
  currentPage: number;
};
export type LinksPaginate = {
  first: string;
  previous: string;
  next: string;
  last: string;
};
